import { AppShell, PageHeading } from "@/components/ChromaShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { Activity, CreditCard, Monitor, RefreshCw, Search, ShieldCheck, Users } from "lucide-react";
import { useMemo, useState } from "react";

function formatDate(value?: Date | string | null) { return value ? new Date(value).toLocaleString("ru-RU", { dateStyle: "medium", timeStyle: "short" }) : "—"; }
function DashboardFrame({ children }: { children: React.ReactNode }) { return <AppShell><div className="container max-w-[1220px] py-8 sm:py-10">{children}</div></AppShell>; }

export function AdminOperationsPage({ title }: { title: "Users" | "Devices" | "Payments" | "Audit logs" }) {
  const [search, setSearch] = useState("");
  const users = trpc.admin.users.useQuery(undefined, { enabled: title === "Users" });
  const devices = trpc.admin.devices.useQuery(undefined, { enabled: title === "Devices" });
  const payments = trpc.admin.payments.useQuery(undefined, { enabled: title === "Payments" });
  const logs = trpc.admin.auditLogs.useQuery(undefined, { enabled: title === "Audit logs" });
  const active = title === "Users" ? users : title === "Devices" ? devices : title === "Payments" ? payments : logs;
  const refresh = () => active.refetch();
  const q = search.toLowerCase().trim();
  const icon = title === "Users" ? Users : title === "Devices" ? Monitor : title === "Payments" ? CreditCard : Activity;
  const Icon = icon;
  const description = title === "Users" ? "Аккаунты, роли и состояние доступа из основной базы." : title === "Devices" ? "Все привязки Loader и время последней активности." : title === "Payments" ? "Источники оплаты и связь платежа с выданной подпиской." : "Хронология входов, выдачи подписок, активаций, загрузок и админских действий.";
  const filteredUsers = useMemo(() => (users.data ?? []).filter(row => `${row.name ?? ""} ${row.email ?? ""} ${row.username ?? ""}`.toLowerCase().includes(q)), [users.data, q]);
  const filteredDevices = useMemo(() => (devices.data ?? []).filter(row => `${row.device.name} ${row.user.email ?? ""} ${row.user.name ?? ""}`.toLowerCase().includes(q)), [devices.data, q]);
  const filteredPayments = useMemo(() => (payments.data ?? []).filter(row => `${row.payment.provider} ${row.payment.providerPaymentId ?? ""} ${row.user.email ?? ""}`.toLowerCase().includes(q)), [payments.data, q]);
  const filteredLogs = useMemo(() => (logs.data ?? []).filter(row => `${row.log.action} ${row.user?.email ?? ""} ${row.user?.name ?? ""} ${row.log.metadata ?? ""}`.toLowerCase().includes(q)), [logs.data, q]);
  return <DashboardFrame><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-start"><PageHeading eyebrow="Панель управления" title={title === "Audit logs" ? "Журнал действий" : title === "Users" ? "Пользователи" : title === "Devices" ? "Устройства" : "Платежи"} description={description} /><Button onClick={refresh} variant="outline" className="h-10 w-fit rounded-xl border-white/10 bg-transparent"><RefreshCw className="mr-2 h-4 w-4" />Обновить</Button></div><div className="mt-8 flex items-center gap-3 rounded-2xl border border-primary/20 bg-primary/[.04] p-4"><Icon className="h-5 w-5 text-primary" /><div className="text-xs text-muted-foreground">Данные загружаются только через защищённые admin tRPC процедуры и не доступны обычным пользователям.</div></div><div className="mt-5 flex items-center gap-2 rounded-xl border border-white/10 bg-white/[.025] px-3"><Search className="h-4 w-4 text-muted-foreground" /><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Поиск по email, имени, действию…" className="h-11 min-w-0 flex-1 bg-transparent text-sm outline-none" /></div><div className="mt-5 overflow-x-auto surface"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-white/10 text-xs text-muted-foreground"><tr>{title === "Users" ? ["Пользователь", "Email", "Роль", "Статус", "Регистрация", "Последний вход"].map(x => <th key={x} className="px-5 py-4 font-medium">{x}</th>) : title === "Devices" ? ["Устройство", "Пользователь", "Статус", "Добавлено", "Последняя активность"].map(x => <th key={x} className="px-5 py-4 font-medium">{x}</th>) : title === "Payments" ? ["Пользователь", "Провайдер", "Сумма", "Статус", "Подписка", "Дата"].map(x => <th key={x} className="px-5 py-4 font-medium">{x}</th>) : ["Время", "Действие", "Пользователь", "Детали"].map(x => <th key={x} className="px-5 py-4 font-medium">{x}</th>)}</tr></thead><tbody>{active.isLoading ? <tr><td colSpan={6} className="px-5 py-12 text-center text-muted-foreground">Загрузка…</td></tr> : title === "Users" ? filteredUsers.map(row => <tr key={row.id} className="border-b border-white/5"><td className="px-5 py-4 font-semibold">{row.name || row.username || `User #${row.id}`}</td><td className="px-5 py-4 text-muted-foreground">{row.email || "—"}</td><td className="px-5 py-4 text-xs uppercase">{row.role}</td><td className="px-5 py-4"><span className={row.status === "active" ? "text-primary" : "text-red-200"}>{row.status}</span></td><td className="px-5 py-4 text-muted-foreground">{formatDate(row.createdAt)}</td><td className="px-5 py-4 text-muted-foreground">{formatDate(row.lastLoginAt || row.lastSignedIn)}</td></tr>) : title === "Devices" ? filteredDevices.map(row => <tr key={row.device.id} className="border-b border-white/5"><td className="px-5 py-4 font-semibold">{row.device.name}<div className="text-xs text-muted-foreground">#{row.device.id}</div></td><td className="px-5 py-4">{row.user.email || row.user.name || `User #${row.user.id}`}</td><td className="px-5 py-4 text-xs uppercase">{row.device.status}</td><td className="px-5 py-4 text-muted-foreground">{formatDate(row.device.createdAt)}</td><td className="px-5 py-4 text-muted-foreground">{formatDate(row.device.lastSeenAt)}</td></tr>) : title === "Payments" ? filteredPayments.map(row => <tr key={row.payment.id} className="border-b border-white/5"><td className="px-5 py-4">{row.user.email || row.user.name || `User #${row.user.id}`}</td><td className="px-5 py-4">{row.payment.provider}<div className="text-xs text-muted-foreground">{row.payment.providerPaymentId || "без внешнего ID"}</div></td><td className="px-5 py-4 font-semibold">{(row.payment.amount / 100).toFixed(2)} {row.payment.currency}</td><td className="px-5 py-4 text-xs uppercase">{row.payment.status}</td><td className="px-5 py-4">#{row.payment.subscriptionId || "—"}</td><td className="px-5 py-4 text-muted-foreground">{formatDate(row.payment.createdAt)}</td></tr>) : filteredLogs.map(row => <tr key={row.log.id} className="border-b border-white/5"><td className="whitespace-nowrap px-5 py-4 text-xs text-muted-foreground">{formatDate(row.log.createdAt)}</td><td className="px-5 py-4 font-semibold"><span className="inline-flex items-center gap-2"><ShieldCheck className="h-3.5 w-3.5 text-primary" />{row.log.action}</span></td><td className="px-5 py-4">{row.user?.email || row.user?.name || (row.log.userId ? `User #${row.log.userId}` : "System")}</td><td className="max-w-[420px] truncate px-5 py-4 text-xs text-muted-foreground" title={row.log.metadata || ""}>{row.log.metadata || "—"}</td></tr>)}{!active.isLoading && ((title === "Users" && !filteredUsers.length) || (title === "Devices" && !filteredDevices.length) || (title === "Payments" && !filteredPayments.length) || (title === "Audit logs" && !filteredLogs.length)) && <tr><td colSpan={6} className="px-5 py-12 text-center text-muted-foreground">Ничего не найдено.</td></tr>}</tbody></table></div></DashboardFrame>;
}
